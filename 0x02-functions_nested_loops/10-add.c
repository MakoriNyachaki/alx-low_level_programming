#include "main.h"
/**
 * add - adds two integers
 * @k: one of the integers
 * @l: Integer
 * Return: returns the value of the sum
 */
int add(int k, int l)
{
	int sum;

	sum = k + l;
	return (sum);
}
